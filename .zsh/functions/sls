# List all available screens

autoload colors && colors

for pipe in /var/run/screen/S-$USER/*; do
    local name=$(basename $pipe)
    echo "${fg_bold[yellow]}$name$reset_color"
    pstree $(echo $name | cut -d. -f1)
    echo
done
