# Colors for tracebacks and object info (linux is brighter)
c.TerminalInteractiveShell.colors = 'linux'

# Pygments syntax highlighting style
c.TerminalInteractiveShell.highlighting_style = 'monokai'

# Show completions in a single column with type on the right
c.TerminalInteractiveShell.display_completions = 'column'

# Leave room for more completions
c.TerminalInteractiveShell.space_for_menu = 10

# Require magic functions to be called explicitly with %
c.InteractiveShell.automagic = False
