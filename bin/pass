#!/bin/bash

ENCDIR=~/Documents/Hush
PLAINDIR=~/Documents/Plain
PASSFILE=pass.txt

set -e

if [[ -z $EDITOR ]]; then
   echo "EDITOR not set" && exit
fi

encfs "$ENCDIR" "$PLAINDIR"
trap "umount \"$PLAINDIR\"" EXIT
$EDITOR "$PLAINDIR/$PASSFILE"
